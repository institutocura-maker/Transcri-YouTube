# 40-devolucao — o que volta para a KB-RC

**O revisor propõe; o curador aplica.** Nada aqui altera `KB-RC/` diretamente.

| Arquivo | Conteúdo |
|---|---|
| `devolucao-a-kb.md` + `.docx` | as 4 seções obrigatórias do Guia §15: novos termos, novas variantes STT, correções na KB, novos registros bibliográficos — mais as divergências factuais reportadas ao produtor |
| `adjudicacao.md` + `.docx` | livro-razão: cada linha da fila de decisão, com o motivo |
| `externos-novos.csv` | linhas prontas para `ferramentas/dados/externos.csv`, no formato do arquivo |

Depois de escrito, os itens devem ser consolidados em `KB-RC/_fila-de-curadoria.csv`, para que
propostas de várias transcrições não se percam.
